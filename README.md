# dotfiles André Lucas

## Install

Clone this repo into `~/.dotfiles`:

    git clone git@github.com:Drelucas/dotfiles.git ~/.dotfiles

Then install the dotfiles:

    cd .dotfiles
    sh install.sh
